# Evaluation Tasks
# 评测任务集

Directory structure for LingXiAgent benchmark tasks (migrated from `Benchmarks/` in P25).
Each task is self-contained under `Evals/Tasks/<id>/task.json` with an executable oracle.
