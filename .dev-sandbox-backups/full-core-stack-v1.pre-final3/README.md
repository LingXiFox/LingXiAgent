This directory is intentionally cassette-free until the one authorized real Provider recording is completed.

`manifest.json`, `provider.jsonl`, and `interactions.jsonl` are generated through a staging directory and promoted only after secret scan and offline replay pass.
