---
name: fixture-analysis
description: Analyze the deterministic VCR fixture and report its two anchors.
---

Read `Fixtures/Foo.swift` and `Fixtures/Bar.swift`. Report `FooAnchor-729` and `BarAnchor-729` exactly. Do not access paths outside the workspace.
